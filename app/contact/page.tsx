export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <main className="page">
      <div className="container">
        <div className="eyebrow">Contact</div>
        <h1>Get in touch</h1>
        <p>For corrections, partnership enquiries or product review requests, email:</p>
        <p><strong>hello@softlixo.com</strong></p>
        <p>Note: this email address must be configured separately before it can receive mail.</p>
      </div>
    </main>
  );
}
